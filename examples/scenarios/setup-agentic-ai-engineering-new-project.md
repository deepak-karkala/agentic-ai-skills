# Scenario: Initializing the Plugin for a New Project

## Trigger

> "I just created a new agent project. How do I configure the agentic-ai-engineering plugin for this repo?"

## Skill: setup-agentic-ai-engineering

**Note:** This skill requires explicit invocation. Use `/agentic-ai-engineering:setup-agentic-ai-engineering`.

## Configuration Flow

The skill checks for `.agentic/config.yml` (not present) and runs the interactive setup flow:

```
Where are your eval configs, traces, and scorecards stored?
Default: evals/
> evals/

Where are your architecture docs, system design docs, and RFCs stored?
Default: docs/architecture
Press Enter to skip.
> docs/architecture

Where should generated HTML and Markdown artifacts be written?
Default: .agentic/artifacts
Press Enter to use the default.
>

Where are your Architecture Decision Records stored?
Default: docs/decisions
Press Enter to skip.
>

Where does your agent implementation code live?
Default: src/agents
Press Enter to skip.
> src/agents

Where are runtime agent traces stored?
Default: logs/traces
Press Enter to skip.
>

Do you have a project glossary or terminology doc?
Path or press Enter to skip.
>

Should .agentic/config.yml be committed to version control?
[y] Yes — shared team config, check it in
[n] No — local only, add to .gitignore
Default: y
> y
```

## Output: .agentic/config.yml written

```yaml
# .agentic/config.yml
# Generated by /agentic-ai-engineering:setup-agentic-ai-engineering
# Version 1 — do not edit field names without running setup again

version: "1"

eval_assets_path: "evals/"
design_docs_path: "docs/architecture"
artifact_output_path: ".agentic/artifacts"
adr_path: ""
agent_source_path: "src/agents"
trace_log_path: ""
glossary_path: ""
```

## Post-Setup Report

```
Setup complete. .agentic/config.yml written.

Hard dependency satisfied:
  eval_assets_path: evals/  ✓

Soft dependencies configured:
  design_docs_path: docs/architecture
  artifact_output_path: .agentic/artifacts
  adr_path: (not set — agentic-system-design will work without ADRs)
  agent_source_path: src/agents
  trace_log_path: (not set — agent-eval-design will run in greenfield mode)
  glossary_path: (not set)

Skills that work fully now: all 5 flagship skills
Skills that improve with more config: agentic-system-design (add adr_path)

To reconfigure, run /agentic-ai-engineering:setup-agentic-ai-engineering again.
```

## What changes in skill behavior after setup

| Skill | Without setup | With this setup |
|---|---|---|
| `agent-eval-design` (audit) | Cannot locate eval assets; blocks audit mode | Reads from `evals/` |
| `agentic-system-design` | Works from user descriptions only | Reads existing docs from `docs/architecture` |
| HTML artifact generation | Writes to `.agentic/artifacts/` (default) | Writes to `.agentic/artifacts/` (explicitly confirmed) |
| Other skills | Fully conversational; unaffected | Same |
